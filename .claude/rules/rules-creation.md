---
paths:
  - ".claude/rules/**/*.md"
---

# 规范文件编写指南

## 文件结构

```markdown
---
paths:
  - "{匹配路径}/**/*.{ext}"
---

# {规范名称}

{一句话描述}

## 目录结构

## 命名规范（如有 precommit 测试）

## 禁止事项（可选）

## 测试规范（可选）

## {具体规范}...
```

## 目录结构格式

````markdown
```
{path}/
├── file.go         # 说明
├── {template}.go   # 占位符用 {}
└── doc.go          # （必需）/（可选）标注
```

**命名约定**:

- `{template}` 占位符说明
````

## 命名规范格式

仅当有 precommit 测试时添加：

```markdown
| 文件      | 类型   | 规则          | 示例      |
| --------- | ------ | ------------- | --------- |
| `file.go` | struct | 以 `Xxx` 结尾 | `UserXxx` |

> 规范由 `internal/precommit/xxx_test.go` 自动检查
```

## 代码示例格式

```go
// ✅ 正确
response.OK(c, "success", data)

// ❌ 禁止
c.JSON(200, gin.H{...})
```

## 内容简洁性原则

规范文档应保持简洁，只包含核心规则和抽象示例。

### 禁止过度展开

| ❌ 禁止            | ✅ 推荐                 |
| ------------------ | ----------------------- |
| 每个子项单独章节   | 合并为一个章节          |
| 详细的模块列举表格 | 抽象示例（如 `{name}`） |
| 冗长的解释文字     | 一句话原则 + 代码       |
| 重复示例变体       | 一个代表性示例          |

### 章节长度控制

- 核心章节：不超过 15 行
- 代码示例：优先抽象示例，不列举所有具体模块
- 表格：仅列关键映射，避免穷举

### 示例对比

````markdown
## ❌ 过度展开

### 使用 Domain 层错误定义

Handler 必须使用 Domain 层定义的错误常量，禁止自定义错误。

### 包别名规范

Domain 层导入统一使用 `d_<name>` 格式别名：
| 模块 | 别名 | 示例 |
| org | d_org | ... |
| role | d_role | ... |
| ... | ... | ... |

### HTTP 状态码映射

| Domain 错误 | HTTP | 前端 |
| ... | ... | ... |
（共 60+ 行）

## ✅ 简洁规范

## 错误处理

Handler 必须使用 Domain 层错误常量，使用 `d_<name>` 别名：

```go
import d_org "github.com/.../internal/domain/org"
if errors.Is(err, d_org.ErrXxx) { ... }
```
````

| Domain 错误  | HTTP 状态 | 前端显示 |
| ------------ | --------- | -------- |
| 业务规则违规 | 400       | 警告     |
| ...          | ...       | ...      |

```

```
