# https://taskfile.dev

version: "3"

vars:
  GREETING: 用于本项目的常用命令编写

tasks:
  default:
    desc: 默认任务
    cmds:
      - echo "${GREETING}"

  prettier:
    desc: 运行 Prettier 进行代码格式化
    cmds:
      - npx prettier --write docs web
